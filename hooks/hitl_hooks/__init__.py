"""
hitl_hooks — Claude Code PreToolUse hooks for enforced HITL approval.

Entry points:
    hitl-hook     Run as a Claude Code PreToolUse hook (reads stdin JSON, exits 0/1)
    hitl-install  Install hook configuration into .claude/settings.json
"""
